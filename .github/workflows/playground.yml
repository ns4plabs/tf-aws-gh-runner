name: Playground

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/playground.yml

jobs:
  run:
    name: Run
    runs-on: [self-hosted, linux, x64, 2xlarge]
    steps:
      - run: sysctl -a
        shell: bash
