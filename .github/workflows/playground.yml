name: Playground

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/playground.yml

jobs:
  playground:
    name: Play
    runs-on: ["self-hosted", "windows", "x64", "playground"]
    steps:
      - run: $env:path -split ";"
      - run: ls "C:\Program Files\Git\usr\bin"
      - run: ls "C:\tools"
      - run: ls "C:\"
      - run: choco install msys2 powershell-core -y
