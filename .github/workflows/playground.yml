name: Playground

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/playground.yml

jobs:
  playground:
    name: Play
    runs-on: ["self-hosted", "linux", "x64", "playground"]
    steps:
      - run: sudo docker info
        shell: bash
