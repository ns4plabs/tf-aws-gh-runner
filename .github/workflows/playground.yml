name: Playground

on:
  workflow_dispatch:

jobs:
  run:
    name: Run
    runs-on: [self-hosted, linux, x64, large-linux-runner]
    steps:
      - name: Exec
        run: pwd && ls
        shell: bash
