name: Playground

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/playground.yml

jobs:
  run:
    name: Run
    runs-on: [self-hosted, linux, x64, test-plans]
    steps:
      - run: |
          docker buildx version
        shell: bash
