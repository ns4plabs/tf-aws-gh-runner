name: Playground

on:
  workflow_dispatch:

jobs:
  run:
    name: Run
    runs-on: [self-hosted, linux, x64, test-plans]
    steps:
      - run: curl -v internal-tf-aws-gh-runner-docker-proxy-669910242.us-east-1.elb.amazonaws.com
        shell: bash
