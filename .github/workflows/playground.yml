name: Playground

on:
  workflow_dispatch:

jobs:
  run:
    name: Run
    runs-on: [self-hosted, windows, x64, large-windows-runner]
    steps:
      - name: Exec
        run: pwd && ls
        shell: bash
