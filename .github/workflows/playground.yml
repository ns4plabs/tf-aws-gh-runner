name: Playground

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/playground.yml

jobs:
  playground:
    name: Play
    runs-on: ["self-hosted", "linux", "x64", "playground"]
    steps:
      - env:
          HELLO: World!
        run: echo "Hello" "$HELLO"
        shell: bash
      - run: sleep 600
        shell: bash
