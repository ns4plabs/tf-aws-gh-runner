name: Playground

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/playground.yml

jobs:
  Run:
    runs-on: ['self-hosted', 'linux', 'x64', 'large']
    name: Run
    steps:
      - run: cmake
        shell: bash
